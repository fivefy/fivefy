spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fivefy_test?serverTimezone=Asia/Seoul
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

kafka:
  bootstrap-servers: localhost:9092,localhost:9093,localhost:9094

jwt:
  secret: fivefy-test-secret-key-for-local-only

app:
  add-wallet-dummy-test-data: false

portone:
  api-secret: test
  store-id: test
  api-url: test
  channel-key: test
  webhook-secret: test